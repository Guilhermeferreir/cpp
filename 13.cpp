#incluse<stdio.h>
#incluse<conio.h>
int main()

{
    float Num, Numchave: inteiro
    printf("digite um numero inteiro);
    scanf("%f" ,&num);
    numchave<-20
    if(num>=0)e(num<=100)então
    num<-(num-numchave);
    if(num<0)então
    printf("a distancia do numero digitado e o numero chave é",num);
    else
    printf("numero digitado incorreto");
    getch();
    return 0;
}
