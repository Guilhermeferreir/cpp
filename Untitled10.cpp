#incluse<stdio.h>
#incluse<conio.h>

int main()
{
    float A, B, C;
    printf("entre com os valores");
    scanf("%f %f %f", A B C);
    if(Valor 1= Valor 2);
    printf("os numeros digitados são iguais, então não há diferença entre eles");
    else
    if(Valor1<valor2);
    scanf(<-Valor2-Valor1)
    printf("a diferença entre os numeros é");
    
    getch();
    return 0;
}
    
